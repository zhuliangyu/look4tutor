<%= simple_form_for [@tutor,@comment] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :rate,:as => :hidden %>
    <%= f.input :body %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
